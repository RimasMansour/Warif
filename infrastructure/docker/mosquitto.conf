# infrastructure/docker/mosquitto.conf
# Eclipse Mosquitto configuration for local development

listener 1883
allow_anonymous true

# WebSocket listener (for browser-based MQTT clients)
listener 9001
protocol websockets

# Logging
log_type all
log_dest stdout

# Persistence (optional — comment out for a clean slate on restart)
persistence true
persistence_location /mosquitto/data/
